--- charts-original/templates/exporters/kube-proxy/servicemonitor.yaml
+++ charts/templates/exporters/kube-proxy/servicemonitor.yaml
@@ -11,7 +11,7 @@
   labels:
     app: {{ template "kube-prometheus-stack.name" . }}-kube-proxy
   {{- with .Values.kubeProxy.serviceMonitor.additionalLabels }}
-    {{- toYaml . | nindent 4 }}
+{{- tpl (toYaml .) $ | nindent 4 }}
   {{- end }}
 {{ include "kube-prometheus-stack.labels" . | indent 4 }}
 spec:
@@ -21,17 +21,24 @@
     {{- toYaml . | nindent 4 }}
   {{- end }}
   {{- include "servicemonitor.scrapeLimits" .Values.kubeProxy.serviceMonitor | nindent 2 }}
+
+  {{- /* Standardized Selector with Zero-Margin and Global Scope */ -}}
   selector:
     {{- if .Values.kubeProxy.serviceMonitor.selector }}
-    {{ tpl (toYaml .Values.kubeProxy.serviceMonitor.selector | nindent 4) . }}
+{{- tpl (toYaml .Values.kubeProxy.serviceMonitor.selector) $ | nindent 4 }}
     {{- else }}
     matchLabels:
       app: {{ template "kube-prometheus-stack.name" . }}-kube-proxy
       release: {{ $.Release.Name | quote }}
     {{- end }}
+
   namespaceSelector:
     matchNames:
       - "kube-system"
+
+  {{- /* Global Check for Rancher Cluster Labels */ -}}
+  {{- $hasExtra := or .Values.global.cattle.clusterId .Values.global.cattle.clusterName -}}
+
   endpoints:
   - port: {{ .Values.kubeProxy.serviceMonitor.port }}
     {{- if .Values.kubeProxy.serviceMonitor.interval }}
@@ -46,12 +53,30 @@
     tlsConfig:
       caFile: /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
     {{- end}}
-{{- if .Values.kubeProxy.serviceMonitor.metricRelabelings }}
+
+    {{- /* Standard Defensive Pattern for MetricRelabelings */ -}}
+    {{- $userMR := (.Values.kubeProxy.serviceMonitor.metricRelabelings | default (list)) -}}
+    {{- $hasUserMR := gt (len $userMR) 0 -}}
+    {{- if or $hasUserMR $hasExtra }}
     metricRelabelings:
-{{ tpl (toYaml .Values.kubeProxy.serviceMonitor.metricRelabelings | indent 4) . }}
-{{- end }}
-{{- if .Values.kubeProxy.serviceMonitor.relabelings }}
+      {{- if $hasUserMR }}
+{{- tpl (toYaml $userMR) $ | nindent 6 }}
+      {{- end }}
+      {{- if .Values.global.cattle.clusterId }}
+      - sourceLabels: [__address__]
+        targetLabel: cluster_id
+        replacement: {{ .Values.global.cattle.clusterId | quote }}
+      {{- end }}
+      {{- if .Values.global.cattle.clusterName }}
+      - sourceLabels: [__address__]
+        targetLabel: cluster_name
+        replacement: {{ .Values.global.cattle.clusterName | quote }}
+      {{- end }}
+    {{- end }}
+
+    {{- /* Optimized Relabelings block with nindent 6 */ -}}
+    {{- with .Values.kubeProxy.serviceMonitor.relabelings }}
     relabelings:
-{{ tpl (toYaml .Values.kubeProxy.serviceMonitor.relabelings | indent 4) . }}
-{{- end }}
-{{- end }}
+{{- tpl (toYaml .) $ | nindent 6 }}
+    {{- end }}
+{{- end }}
\ No newline at end of file
