--- charts-original/templates/exporters/kube-scheduler/servicemonitor.yaml
+++ charts/templates/exporters/kube-scheduler/servicemonitor.yaml
@@ -11,7 +11,7 @@
   labels:
     app: {{ template "kube-prometheus-stack.name" . }}-kube-scheduler
   {{- with .Values.kubeScheduler.serviceMonitor.additionalLabels }}
-    {{- toYaml . | nindent 4 }}
+{{- tpl (toYaml .) $ | nindent 4 }}
   {{- end }}
 {{ include "kube-prometheus-stack.labels" . | indent 4 }}
 spec:
@@ -21,17 +21,24 @@
     {{- toYaml . | nindent 4 }}
   {{- end }}
   {{- include "servicemonitor.scrapeLimits" .Values.kubeScheduler.serviceMonitor | nindent 2 }}
+
+  {{- /* Standardized Selector with Zero-Margin */ -}}
   selector:
     {{- if .Values.kubeScheduler.serviceMonitor.selector }}
-    {{ tpl (toYaml .Values.kubeScheduler.serviceMonitor.selector | nindent 4) . }}
+{{- tpl (toYaml .Values.kubeScheduler.serviceMonitor.selector) $ | nindent 4 }}
     {{- else }}
     matchLabels:
       app: {{ template "kube-prometheus-stack.name" . }}-kube-scheduler
       release: {{ $.Release.Name | quote }}
     {{- end }}
+
   namespaceSelector:
     matchNames:
       - "kube-system"
+
+  {{- /* Define cluster extras once at the spec level */ -}}
+  {{- $hasExtra := or .Values.global.cattle.clusterId .Values.global.cattle.clusterName -}}
+
   endpoints:
   - port: {{ .Values.kubeScheduler.serviceMonitor.port }}
     {{- if .Values.kubeScheduler.serviceMonitor.interval }}
@@ -52,12 +59,30 @@
       serverName: {{ .Values.kubeScheduler.serviceMonitor.serverName }}
       {{- end}}
     {{- end}}
-{{- if .Values.kubeScheduler.serviceMonitor.metricRelabelings }}
+
+    {{- /* Standard Defensive Pattern for MetricRelabelings */ -}}
+    {{- $userMR := (.Values.kubeScheduler.serviceMonitor.metricRelabelings | default (list)) -}}
+    {{- $hasUserMR := gt (len $userMR) 0 -}}
+    {{- if or $hasUserMR $hasExtra }}
     metricRelabelings:
-{{ tpl (toYaml .Values.kubeScheduler.serviceMonitor.metricRelabelings | indent 4) . }}
-{{- end }}
-{{- if .Values.kubeScheduler.serviceMonitor.relabelings }}
+      {{- if $hasUserMR }}
+{{- tpl (toYaml $userMR) $ | nindent 6 }}
+      {{- end }}
+      {{- if .Values.global.cattle.clusterId }}
+      - sourceLabels: [__address__]
+        targetLabel: cluster_id
+        replacement: {{ .Values.global.cattle.clusterId | quote }}
+      {{- end }}
+      {{- if .Values.global.cattle.clusterName }}
+      - sourceLabels: [__address__]
+        targetLabel: cluster_name
+        replacement: {{ .Values.global.cattle.clusterName | quote }}
+      {{- end }}
+    {{- end }}
+
+    {{- /* Optimized Relabelings block with nindent 6 */ -}}
+    {{- with .Values.kubeScheduler.serviceMonitor.relabelings }}
     relabelings:
-{{ tpl (toYaml .Values.kubeScheduler.serviceMonitor.relabelings | indent 4) . }}
-{{- end }}
-{{- end }}
+{{- tpl (toYaml .) $ | nindent 6 }}
+    {{- end }}
+{{- end }}
\ No newline at end of file
